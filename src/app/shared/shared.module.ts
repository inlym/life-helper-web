import {NgModule} from '@angular/core'
import {CommonModule} from '@angular/common'

/**
 * 共享模块
 *
 * ## 主要用途
 * 管理在应用中被多处使用的组件、指令、管道和其他共享代码，对其统一导入和导出。
 */
@NgModule({
  declarations: [],
  imports: [CommonModule],
})
export class SharedModule {}
